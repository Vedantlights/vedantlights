VITE_BACKEND_ORIGIN=https://vedantlights.com
# XAMPP at localhost/vedantlights: use /vedantlights. Production at domain root: use empty.
VITE_BACKEND_BASE_PATH=/
# Must match app base path + /react-app/dist/ so assets resolve. XAMPP: /vedantlights/
# Production at domain root: use VITE_PUBLIC_PATH=/
VITE_PUBLIC_PATH=/react-app/dist/
VITE_BUILD_OUT_DIR=dist

